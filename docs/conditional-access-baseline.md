# Conditional Access Baseline

- Require MFA for administrators.
- Require MFA for high-value applications.
- Block legacy authentication after impact review.
- Require compliant or managed devices for sensitive data paths where supported.
- Exclude documented break-glass accounts from blocking policies, then monitor them heavily.
